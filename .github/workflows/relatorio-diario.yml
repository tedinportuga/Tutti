name: Relatorio Diario

on:
  schedule:
    - cron: '59 22 * * *'
  workflow_dispatch:

jobs:
  relatorio:
    runs-on: ubuntu-latest
    steps:
      - name: Enviar relatorio
        run: |
          curl -X GET "https://tuttipizzaria.netlify.app/api/export"
